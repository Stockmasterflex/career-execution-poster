---
layout: base.njk
title: "Career Execution Poster"
permalink: "/poster/"
---

{% set meta = plan.meta %}
{% set phases = plan.phases %}
{% set daily = plan.daily %}
{% set weekly = plan.weekly %}
{% set budget = plan.budget %}
{% set milestones = plan.milestones %}

<!-- Hero Section -->
<div class="section">
    <h1>{{ meta.title }}</h1>
    <a class="pill" href="/calendar/">Open full Weekly Calendar →</a>
    <div class="grid grid-4">
        <div class="card">
            <h3>Location</h3>
            <p class="highlight">{{ meta.location }}</p>
        </div>
        <div class="card">
            <h3>Timeline</h3>
            <p class="highlight">{{ meta.timeline }}</p>
        </div>
        <div class="card">
            <h3>North Star</h3>
            <p class="success">{{ meta.northStar }}</p>
        </div>
        <div class="card">
            <h3>Runway</h3>
            <p class="warning">{{ meta.runway }}</p>
        </div>
    </div>
    <div class="grid grid-2">
        <div class="card">
            <h3>Target Start</h3>
            <p>{{ meta.targetStart }}</p>
        </div>
        <div class="card">
            <h3>Schedule</h3>
            <p>Sleep: {{ meta.sleep }}<br>Market: {{ meta.marketHours }}</p>
        </div>
    </div>
</div>

<!-- Phases Section -->
<div class="section">
    <h2>Execution Phases</h2>
    <div class="grid grid-2">
        {% for phase in phases %}
        <div class="card">
            <h3>{{ phase.title }}</h3>
            <p class="highlight">{{ phase.dates }}</p>
            <h4>Goals:</h4>
            <ul class="tier-list">
                {% for goal in phase.goals %}
                <li>{{ goal }}</li>
                {% endfor %}
            </ul>
        </div>
        {% endfor %}
    </div>
</div>

{% for ph in phases %}
  {% if ph.tiers %}
    {% include "sections/tier-grid.njk" %}
  {% endif %}
{% endfor %}

<!-- Daily Section -->
<div class="section">
    <h2>Daily Routine</h2>
    <div class="grid grid-2">
        <div class="card">
            <h3>Morning ({{ daily.morning | length * 10 }} min)</h3>
            <ul class="tier-list">
                {% for activity in daily.morning %}
                <li>{{ activity.activity }} ({{ activity.minutes }}min)</li>
                {% endfor %}
            </ul>
        </div>
        <div class="card">
            <h3>Evening ({{ daily.evening | length * 20 }} min)</h3>
            <ul class="tier-list">
                {% for activity in daily.evening %}
                <li>{{ activity.activity }} ({{ activity.minutes }}min)</li>
                {% endfor %}
            </ul>
        </div>
    </div>
</div>

<!-- Weekly Section -->
{% include "sections/weekly.njk" %}

<!-- Budget Section -->
<div class="section">
    <h2>Budget Allocation</h2>
    <div class="grid grid-2">
        <div class="card">
            <h3>Living Expenses</h3>
            <p class="highlight">{{ budget.living | currency }}</p>
        </div>
        <div class="card">
            <h3>Professional Development</h3>
            {% for category, amount in budget.professional %}
            <div class="budget-item">
                <span>{{ category | replace("_", " ") | title }}</span>
                <span>{{ amount | currency }}</span>
            </div>
            {% endfor %}
            {% set p = budget.professional %}
            <div class="budget-item" style="border-top: 2px solid #444; font-weight: 600;">
                <span><strong>Total Professional</strong></span>
                <strong>{{ (p.cfa_cmt + p.events + p.branding + p.coaching + p.wardrobe) | currency }}</strong>
            </div>
        </div>
    </div>
</div>

<!-- Milestones Section -->
<div class="section">
    <h2>Key Milestones</h2>
    {% for level, data in milestones %}
    <div class="milestone">
        <div>
            <h3>{{ level | replace("level", "Level ") | title }}</h3>
            <ul style="margin: 0.5rem 0; padding-left: 1rem;">
                {% for item in data.items %}
                <li>{{ item }}</li>
                {% endfor %}
            </ul>
        </div>
        <div class="highlight">{{ data.date }}</div>
    </div>
    {% endfor %}
</div>
